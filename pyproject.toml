[project]
name = "call-me-maybe"
version = "0.0.1"
description = "Integer AI in a project"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.4.6",
    "pydantic>=2.13.4",
    "llm_sdk",
]

[tool.uv.sources]
llm_sdk = { path = "./llm_sdk" }